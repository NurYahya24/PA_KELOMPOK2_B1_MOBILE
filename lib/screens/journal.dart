import 'package:flutter/material.dart';

class journal_page extends StatelessWidget {
  const journal_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Laman Jurnal'),
        ),
      ),
    );
  }
}
