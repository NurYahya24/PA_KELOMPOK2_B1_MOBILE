import 'package:flutter/material.dart';

class highlight_page extends StatelessWidget {
  const highlight_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Laman Highlight'),
        ),
      ),
    );
  }
}
