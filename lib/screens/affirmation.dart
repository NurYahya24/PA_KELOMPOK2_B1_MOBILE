import 'package:flutter/material.dart';

class affirmation_page extends StatelessWidget {
  const affirmation_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Laman Afirmasi'),
        ),
      ),
    );
  }
}
