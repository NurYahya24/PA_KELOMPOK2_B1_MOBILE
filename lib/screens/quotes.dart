import 'package:flutter/material.dart';

class quotes_page extends StatelessWidget {
  const quotes_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Laman Quotes'),
        ),
      ),
    );
  }
}
